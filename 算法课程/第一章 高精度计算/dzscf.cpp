#include <bits/stdc++.h>
using namespace std;
char a[200], b[200],c[200];
sub(){
    // 减法
}
int main()
{
    char ai[200],bi[200];
    cin >> ai >> bi;
    // 倒序存储a,b
    
    // 比较a,b，判断谁是被除数

    int c;
    while (compare(a,b))
    {
        sub(a,b);
        c++;
    }
    
    cout<<c;
}
